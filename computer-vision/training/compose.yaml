services:
  trainer:
    build:
      context: .
      dockerfile: Dockerfile
    # runtime: nvidia
    # ipc: host
    # ulimits:
    #   memlock: -1
    #   stack: 67108864
    environment:
      EPOCHS: 10
      # EXPORT_FORMATS: "engine,onnx"
    volumes:
      - ./data:/ultralytics/data
